
FROM httpd:v5

COPY conf/httpd.conf /usr/local/apache2/conf/httpd.conf

#RUN apt-get update && apt-get install -y vim


apt-get update \
  && apt-get install -y curl vim less \
  && apt-get install -y ca-certificates-java \
  && apt-get install -y openjdk-8-jre-headless openjdk-8-jre openjdk-8-jdk \
  && apt-get install -y logstash

curl -L -O https://artifacts.elastic.co/downloads/beats/metricbeat/metricbeat-6.2.4-amd64.deb

dpkg -i metricbeat-6.2.4-amd64.deb


